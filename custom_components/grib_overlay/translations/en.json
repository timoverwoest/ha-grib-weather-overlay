{
  "config": {
    "step": {
      "user": {
        "title": "GRIB Weather Overlay",
        "description": "Choose a GRIB data source and enter your API key.",
        "data": {
          "source": "Source",
          "api_key": "API key",
          "notification_api_key": "Notification Service API key (optional)"
        },
        "data_description": {
          "notification_api_key": "Optional: a separate KNMI Notification Service key for instant push updates of new forecast runs. Leave empty to rely on periodic polling."
        }
      },
      "dataset": {
        "title": "Choose a dataset",
        "data": {
          "dataset": "Dataset"
        }
      },
      "parameters": {
        "title": "Choose parameters",
        "description": "Select which weather parameters should be kept up to date and made available to the map card.",
        "data": {
          "parameters": "Parameters"
        }
      }
    },
    "error": {
      "invalid_auth": "The API key was rejected by the source.",
      "cannot_connect": "Could not reach the source's API.",
      "no_parameters_selected": "Select at least one parameter."
    },
    "abort": {
      "already_configured": "This source/dataset combination is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "GRIB Weather Overlay options",
        "data": {
          "forecast_horizon_hours": "Forecast horizon (hours)",
          "retain_runs": "Number of forecast runs to keep cached",
          "update_interval_minutes": "Polling interval (minutes)",
          "notification_api_key": "Notification Service API key (optional)",
          "observations_api_key": "Observations API key (optional)",
          "alias": "Short alias (optional)",
          "color_scales": "Custom colour scales (optional)"
        },
        "data_description": {
          "notification_api_key": "Optional: a separate KNMI Notification Service key for instant push updates. Leave empty to rely on periodic polling.",
          "observations_api_key": "Optional: a KNMI Open Data key with access to the '10-minute-in-situ-meteorological-observations' dataset, used to download station observations. Your HARMONIE key is often not authorised for it (KNMI returns 403). Leave empty to reuse the HARMONIE key.",
          "alias": "Optional short name shown as this source's compact label in the comparison and meteogram views, e.g. `KNMI NL`. Leave empty to derive it automatically from the source (same-source entries are disambiguated automatically).",
          "color_scales": "One parameter per line, e.g. `wind_10m: 0:#2c7fb8, 8:#7fcdbb, 12:#ffffb2, 16:#fd8d3c, 24:#bd0026`. Values are in the parameter's own unit (m/s, °C, hPa, mm, m). Your parameters: {parameters}. Leave empty to use the built-in colours. Changing this re-renders the current run."
        }
      }
    }
  }
}
